# zshenv is loaded for interactive (before zshrc) and non-interactive shells

# Shared environment for Bash, Zsh, and Unix installer entry points.
. ~/.shellenv

# Runtime integrations mutate shell state; static values and paths stay in external and bootstrap.
. ~/.config/zsh/integrations.zsh

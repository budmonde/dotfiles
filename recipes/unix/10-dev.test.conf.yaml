schema_version: 3

checks:
  cmake:
    commands:
      version: [cmake --version]

  make:
    commands:
      version: [make --version]

  gcc:
    paths: [gcc, g++]
    commands:
      version: [gcc --version]

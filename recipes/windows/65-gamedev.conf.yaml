# Recipe: 65-gamedev — game development tools
# Usage: .\install.ps1 --recipe gamedev

- install:
    - [install/windows/winget/unity-hub.ps1, Installing Unity Hub]

- shell:
    - command: powershell -NonInteractive -Command "shellver bump machine"
      description: Refreshing Shellver machine generation

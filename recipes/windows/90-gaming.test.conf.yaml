schema_version: 3

checks:
  steam:
    commands:
      installed:
        - "if (((winget list --id Valve.Steam 2>$null) | Out-String) -match 'Valve.Steam') { 'installed' } else { 'missing' }"
        - installed

schema_version: 3

checks:
  unity-hub:
    commands:
      installed:
        - "if (((winget list --id Unity.UnityHub 2>$null) | Out-String) -match 'Unity.UnityHub') { 'installed' } else { 'missing' }"
        - installed

# Shared Node.js configuration applied before the platform-specific fragment.

- defaults:
    link:
        create: true
        relink: true

- clean:
    - ~/.config

- link:
    ~/.config/npm: config/npm

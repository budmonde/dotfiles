# Shared agentic configuration applied before the platform-specific fragment.

- defaults:
    link:
        create: true
        relink: true

- clean:
    - '~'
    - ~/.config
    - ~/.config/claude
    - ~/.config/codex
    - ~/.config/opencode

- link:
    ~/.agents: config/agents
    ~/.config/codex/AGENTS.md: config/agents/AGENTS.md
    ~/.config/codex/config.toml: config/codex/config.toml
    ~/.config/claude/settings.json: config/claude/settings.json
    ~/.config/claude/keybindings.json: config/claude/keybindings.json

    ~/.config/opencode: config/opencode

- install:
    - [install/shared/codex.py, Installing Codex CLI, "0.149.1"]
    - [install/shared/claude-code.py, Installing Claude Code]
    - [install/shared/git-auditor.py, Installing Git-auditor]

- shell:
    - [git config core.hooksPath .githooks, Selecting the common repository hook system]
